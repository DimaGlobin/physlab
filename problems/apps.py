"""App configuration for the problems app."""
from django.apps import AppConfig


class ProblemsConfig(AppConfig):
    """Configuration for the problems application."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'problems'
